

describe('MvnApp controller ', function() {
   /* beforeEach(module('mavenApp'));
    beforeEach(function() {
        module('mavenApp', function($provide) {
            $provide.constant("config", {});
        });
    });
   
    var $controller, $rootScope, $httpBackend, metaRequestHandler;
  
    beforeEach(inject(function(_$controller_, _$rootScope_, $injector){
      $httpBackend = $injector.get('$httpBackend');
      // The injector unwraps the underscores (_) from around the parameter names when matching
      $controller = _$controller_;
      $rootScope = _$rootScope_;

      metaRequestHandler = $httpBackend.when('GET', '/api/v1/_/metadata')
      .respond({"platform_data": {"ios": {"version": "1.2.0"}}, "stripe_publishable_key": "", "ziggeo_account_id": "123", "cx_practitioner_id": 0, "default_college_vertical_group": "college", "tokbox_api_key": "", "messaging_member_character_limit": 500}
    )
      
    }));

    afterEach(function() {
      $httpBackend.verifyNoOutstandingExpectation();
      $httpBackend.verifyNoOutstandingRequest();
    });
  */
  /*describe('$scope.setUser', function() {
    it('sets the rootScope user property', function() {
      var $scope = $rootScope.$new();
      var controller = $controller('MvnCtrl', { $scope: $scope });
      var dummyUser =  {
          first_name: "Jane",
          last_name: "Doe",
          email: "foo@test.com",
          id: 123
      }

      $scope.setUser(dummyUser);
      expect($scope.user.first_name).toEqual('Jane');
    });
  });*/
  });
